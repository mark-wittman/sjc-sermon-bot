# Sermon Bot Dependencies
# Install with: pip install -r requirements.txt

# Phase 1: RSS parsing
feedparser>=6.0

# Phase 2: Audio downloading
requests>=2.28
tqdm>=4.64           # Progress bars for downloads

# Phase 3: Transcription
# Uses whisperkit-cli (Apple Silicon optimized): brew install whisperkit-cli

# Phase 4: Embedding & vector storage
chromadb>=0.4
sentence-transformers>=2.2  # Free local embeddings
langchain>=0.1
langchain-text-splitters>=0.0.1

# Phase 5: Chatbot
anthropic>=0.39      # Claude API client

# Future: Web UI
# streamlit>=1.30   # Uncomment when ready for Phase 5+
